# internal/matcher/league_features.go 函数索引

> 自动生成于 2026-05-16 | 总行数: 715 | 函数数: 1 | 语言: go
> **本文件由 code-indexer 脚本自动生成，严禁手动编辑。**

**巨型函数警告**: 本文件包含 1 个超过 200 行的函数，建议优先通过 `@section` 标记进行内部导航。

## 函数列表

> 定位方式：在源文件中 `grep -n "函数名"` 即可跳转，行号不在此列出（行号随代码变化而失效）。

| 函数名 | 类型 | 签名 | 备注 |
|--------|------|------|------|
| containsWholeWord | function | `containsWholeWord()` | ⚠️ 巨型函数，见 @section 导航 |

## 巨型函数内部节点 (@section 标记)

### containsWholeWord

> 定位：`grep -n '@section:{}'` 跳转到对应节点

| 节点标记 | 说明 |
|----------|------|
| `@section:gender_veto` | 性别强约束一票否决 |
| `@section:age_group_veto` | 年龄段强约束一票否决 |
| `@section:region_veto` | 区域分区强约束一票否决 |
| `@section:competition_type_veto` | 赛制类型强约束一票否决 |
| `@section:tier_number_veto` | 层级数字强约束一票否决 |
