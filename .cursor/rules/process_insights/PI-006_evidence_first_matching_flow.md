---
id: "PI-006"
version: "v1.38"
last_updated: "2026-05-16"
author: "Manus AI, Claude Cowork"
related_modules: ["internal/matcher", "internal/db", "cmd", "docs"]
status: "active"
---

# PI-006: Evidence-First 比赛级匹配流程

## 流程概述

Evidence-First P3 将 P2 输出的 **多 competition TS 比赛候选池** 转化为高置信、一对一的比赛匹配结果。该流程不再假设输入来自某一个 TS 联赛的全部比赛，而是把每条候选比赛作为一条可解释的证据边进行打分，并在自动确认前执行全局冲突消解，确保一个 `ts_match_id` 最多被一个源侧事件占用。

## 核心防坑指南

### 坑 1: 把 P2 候选池重新退化为单联赛全量比赛

**现象**：P2 已经基于联赛候选、球队候选和强约束生成跨 competition 的比赛候选池，但 P3 如果继续调用只接受 `[]db.TSEvent` 的联赛内 `MatchEvents`，会丢失 `competition_id`、P2 先验分和强约束解释，导致联赛名称歧义样本无法提升召回。

**根因**：旧流程在进入比赛匹配时已经确认唯一 TS competition，因此 `db.TSEvent` 不携带 `competition_id`。Evidence-First 流程中，比赛证据必须保留候选来源 competition，否则 P4 无法按联赛聚合、回灌反向确认率。

**正确做法**：使用 `EvidenceEventCandidate` 包装 TS 比赛，并显式传递 `competition_id`、`competition_name`、P2 候选先验分、主客队候选分和强约束结果。输出 `ResolvedEventMatch` 时继续保留 `ts_match_id`、`ts_competition_id`、主客队、时间、置信度、规则和 reason code。

**关键位置**：`internal/matcher/evidence_event_matcher.go` → `EvidenceEventCandidate`、`EventEvidenceEdge`、`ResolvedEventMatch`。

### 坑 2: 先逐源选择最佳候选会造成隐性一对多

**现象**：两个源侧比赛都把同一条 TS 比赛作为最佳候选，若逐条 source 独立确认，最终会出现多个源侧事件匹配同一个 `ts_match_id`。

**根因**：旧版 `MatchEvents` 通过 `usedTSIDs` 在联赛内顺序占用 TS 事件，但 Evidence-First 候选池来自多个 competition，候选边需要先统一排序再消解，否则输入顺序会影响自动确认结果。

**正确做法**：先生成所有 `EventEvidenceEdge`，再按分数降序做贪心一对一消解。短期实现中每条源侧事件和每个 `ts_match_id` 都只能被占用一次；被淘汰候选必须记录 `lost_to`、`winner_score`、`loser_score`、`score_gap` 和淘汰原因。长期可替换为 Hungarian 或 min-cost max-flow，但输出解释字段应保持兼容。

**关键位置**：`internal/matcher/evidence_event_matcher.go` → `resolveConflicts`、`ConflictElimination`。

### 坑 3: 主客反转不能静默等价于正向匹配

**现象**：某些数据源存在主客标注反转，完全禁止反转会漏召回；但把反转候选当作正向候选会提高主客错配风险。

**根因**：主客方向是比赛实体匹配的强语义证据。反转候选可以作为证据保留，但必须进入可解释降权路径。

**正确做法**：同时计算正向和反向主客名称相似度。当反向更强时保留候选边，扣除反转惩罚，并在 `reason_codes` 中加入 `SIDE_REVERSED`。后续 P4 或人工复核可直接识别该风险。

**关键位置**：`internal/matcher/evidence_event_matcher.go` → `scoreEdge`。

### 坑 4: ±24h/±72h 时间偏移不能仅靠硬窗口处理

**现象**：时区或赛程同步错误会造成整批比赛统一偏移，单场高斯时间衰减会把候选压到阈值以下。

**根因**：单场时间差不能区分真实统一偏移和随机错误。旧流程中 EventDTW 是兜底模块，Evidence-First P3 仍应复用该时间序列锚点能力。

**正确做法**：在候选池上构造 DTW 事件序列，调用 `EventDTWMatcher.TryCorrect` 估计源侧整体时间偏移；若偏移可信，则使用修正后的时间差参与 L1-L4/L4b 评分，并在 reason code 中加入 `DTW_OFFSET`。

**关键位置**：`internal/matcher/evidence_event_matcher.go` → `estimateDTWOffset`。

### 坑 5: L4b 队伍 ID 兜底必须依赖两轮流程

**现象**：第一轮还没有稳定的 `teamIDMap`，过早使用队伍 ID 兜底会把噪声 ID 映射放大为比赛误匹配。

**根因**：L4b 的安全性来自第一轮高置信比赛推导出的球队映射，而不是候选池自身。

**正确做法**：`MatchTwoRound` 第一轮只用名称、时间、别名和候选先验打分；随后调用现有 `DeriveTeamMappings` 推导 `teamIDMap`；第二轮把 `teamIDMap` 注入 `Match`，允许 `TEAM_ID_FALLBACK` 兜底极端时间偏移或低名称相似样本。

**关键位置**：`internal/matcher/evidence_event_matcher.go` → `MatchTwoRound`、`hasTeamIDAnchor`。

## 关键耦合点

| 耦合点 | 说明 |
|--------|------|
| `MatchEvents` / L1-L4b | Evidence-First P3 复用现有 `levelConfigs`、`gaussianTimeFactor`、`RuleEventL1`~`RuleEventL4b`，避免另起孤立规则体系。 |
| `TeamAliasIndex` | 候选边使用 `NameSimWithAlias`，未来可注入持久化别名后继续复用别名命中加分和 reason code。 |
| `FSModel` | 候选边将主队相似度、客队相似度、时间差、联赛层级、运动类型转换为 FS 比较向量，作为综合分的一部分。 |
| `EventDTW` | 对候选池整体估计时间偏移，修正后再进入比赛边打分。 |
| `DeriveTeamMappings` | 两轮逻辑沿用现有比赛推导球队映射能力，第二轮启用 L4b 队伍 ID 兜底。 |
| P4 聚合 | `ResolvedEventMatch` 必须保留 `ts_competition_id`、`ts_match_id`、`reason_codes` 和冲突淘汰解释，供联赛级反向确认和人工复核使用。 |

## 版本变更日志

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-04-30 | 初始记录 Evidence-First P3 比赛级候选边打分、一对一冲突消解、主客反转降权、DTW 偏移修正和两轮 L4b 兜底流程。 | Manus AI |
| v1.1 | 2026-05-16 | 落地 P1 候选池生成器 (`internal/matcher/candidate_pool.go`)：`CandidatePoolBuilder` 把 Top-N 联赛候选 + 联赛先验分 + 强约束转化为 `[]EvidenceEventCandidate`。**P1 不做配对/打分/筛选**（P3 职责）。`TSEventLoader` 最小接口 + 并行 goroutine fetch fallback；新增 P0BackwardCompat 单测。 | Claude Cowork |
| v1.2 | 2026-05-16 | 落地 P2 球队级先验填充 (`team_prior_enricher.go` / `EnrichTeamPriors`)：对每个 EvidenceEventCandidate 扫描全部 SR 球队名，取最佳相似度填入 HomeTeamCandidateScore / AwayTeamCandidateScore；别名感知；纯函数原地修改。P3 隐性 contract：`resolveConflicts` 每个 SR 都输出 ResolvedEventMatch（含 NoMatch stub）。 | Claude Cowork |
| v1.3 | 2026-05-16 | 落地联赛 Top-N 入口 (`league_topn.go` / `MatchLeagueTopN` + `ToTSCompetitionCandidates`)。KnownLeagueMap 命中保 Top-1，其余按 leagueNameScore 降序，每条携带 StrongConstraintOK。不破坏 P0（既有 MatchLeague 保留）。 | Claude Cowork |
| v1.4 | 2026-05-16 | Wire Evidence-First 流水线进 `UniversalEngine.RunLeague`：`TopNAdapter` 可选接口 + `SRSourceAdapter.MatchLeagueTopN` + `runLeagueEvidenceFirst` 主体 + `UseEvidenceFirst` / `EvidenceFirstTopN` 两个 opt-in 字段。RunLeague 仅加 4 行 type-assertion 分支，默认 false 走原 P0。 | Claude Cowork |
| v1.5 | 2026-05-16 | CLI 暴露 `--use-evidence-first` 和 `--evidence-first-topn N` 到 4 个生产命令（match2/batch2/ls-match/ls-batch）。 | Claude Cowork |
| v1.6 | 2026-05-16 | LS 链路镜像 (`league_topn_ls.go` / `LSMatchLeagueTopN`)：KnownLSLeagueMap 命中占 Top-1，lsLeagueNameScore 排序，LS 特有 `lsLocationVeto` / `lsLocationVetoByName` 显式记录在 strong constraint reason。`LSSourceAdapter.MatchLeagueTopN` 转发并 propagate NoKnownMap。SR 与 LS 完全对称。 | Claude Cowork |
| v1.7 | 2026-05-16 | P3 scoreEdge time-window 预筛：4 次 NameSimWithAlias 之前用 `correctedDiff > l5MaxTimeDiff (30 天)` + 无 teamIDAnchor 提前 false。严格子集裁剪不改结果集。3 个单测（SkipsFar / KeepsClose / KeepsFarWithAnchor）。 | Claude Cowork |
| v1.8 | 2026-05-16 | P3 NameSim 缓存 + P1 并行 goroutine fetch：`buildNameSimCache` 预计算 unique (srTeamID, tsTeamID) 对，scoreEdge 4 次 NameSim 改为 O(1) 查表 — EPL P3 stage >20s → 1s；P1 串行 4 个 competition 30s → 并行 21s。EPL 实测 v1.5 >40s → v1.8 30.6s。 | Claude Cowork |
| v1.9 | 2026-05-16 | DB batch SQL：`*db.TSAdapter.GetEventsBatch` + `GetTeamNamesBatch` (WHERE competition_id IN (...))；`BatchTSEventLoader` 可选接口。Loader 实现则 batch，否则回退到 v1.8 并行 goroutine。EPL P1 fetch 21s → 12s。 | Claude Cowork |
| v1.10 | 2026-05-16 | **EF 真数据有效性首次正面验证**（沙箱真 DB 跑 3 个高歧义非 KnownMap SR 联赛）。**关键 case：sr:tournament:929 Jordan League** — P0 联赛到 'Jordan League Division 1' (conf=0.908) 但 0/78 events matched；EF TopN=5 同 Top-1 league，但跨 5 个候选 competition 拉了 1845 events，最终 76/78 matched (97.4%)。Premier Soccer League Zimbabwe / Serie B Women Italy 都是 P0 高 conf league 但 0/N events，EF `edges=0` 显式暴露错误。报告：`docs/evidence_first_real_data_validation.md`。 | Claude Cowork |
| v1.11 | 2026-05-16 | `--max-runtime` 软超时：`UniversalEngine.MaxRuntime` + 4 个 CLI 命令 `--max-runtime` flag + `runLeagueEvidenceFirst` 各阶段入口 `budgetExceeded` 检查 → 超时输出当前最佳结果 `Stats.Truncated=true` + `TruncatedStage=topn\|p1\|p2\|p3`。**结构性局限**：budget check 只在 Go 栈空闲点工作，长跑 SQL 卡在 DB 调用里检查不到。3 个新单测。 | Claude Cowork |
| v1.12 | 2026-05-16 | **edges=0 → LEAGUE_SUSPECT 自动降级**：若 `len(efResult.Edges)==0 && league.Matched && != RuleLeagueKnown`，confidence × 0.5、Rule → `RuleLeagueSuspect`。自动捕获 v1.10 Case A/C 类型的 P0 silent 误匹配。沙箱真数据验证 Zimbabwe: NAME_HI 0.890 → SUSPECT 0.445。7 个单测。 | Claude Cowork |
| v1.13 | 2026-05-16 | SQL 时间窗下推（opt-in）：`GetEventsBatchInRange` (WHERE match_time BETWEEN tMin AND tMax) + `TimeRangeBatchTSEventLoader` + `BuildWithTimeRange` + `UniversalEngine.EvidenceFirstTimePadding` + CLI `--ef-time-padding`。Jordan TopN=5: 1845→252 events, 24s→13s (45% 加速), 76/78 不变。EPL: ±30d 217/225 (丢 6 L4b 跨季)，±180d 同样。**默认 0 不下推保 v1.12 行为**，用户按需 opt-in。 | Claude Cowork |
| v1.14 | 2026-05-16 | Context cancellation wire 进 SQL：让 `--max-runtime` 真正能打断长跑 SQL（堵 v1.11 局限）。三层 ctx-aware：(1) `*db.TSAdapter` 加 `GetEventsBatchCtx` / `GetEventsBatchInRangeCtx` / `GetTeamNamesBatchCtx` 用 `db.QueryContext`；(2) `ContextBatchTSEventLoader` + `ContextTimeRangeBatchTSEventLoader` 可选接口；(3) `BuildCtx` / `BuildWithTimeRangeCtx` + `runLeagueEvidenceFirst` 头部 `context.WithDeadline(t0+MaxRuntime)`。stub 不实现 ctx-aware 自动回退。3 个单测。 | Claude Cowork |
| v1.15 | 2026-05-16 | Direction D + E 双优化：(D) `UniversalEngine.tsCompCache` 缓存 `GetCompetitionsByFootball/Basketball` 跨 RunLeague 复用 + `InvalidateCompetitionCache()`。batch2 跨 29 联赛累计省 ~29s。(E) `BuildCtx` 与 `BuildWithTimeRangeCtx` 把 events + teamNames fetch 改为 sync.WaitGroup 并行 2 goroutine，单 RunLeague P1 fetch 串行 ~7s → 并行 ~4s。不改语义、不影响结果集。 | Claude Cowork |
| **v1.16** | **2026-05-16** | **球队优先兜底路径（Team-First Fallback）— 沙箱端到端验证！** Zimbabwe Premier Soccer League: P0=0/75（BPL conf=0.890 silent 误匹配）→ EF v1.15=0/75（edges=0 SUSPECT）→ **EF+team-first=66/75 (88%) Rule=LEAGUE_TEAM_FIRST**（找到真 Zimbabwe 联赛 `4zp5rzgh7loq82w`，TS 球队名带 `(ZWE)` 标）。沙箱完整 30.5s 跑完。实现栈：(1) `db.TSAdapter` 加 `GetAllTeamsBriefCtx` / `GetEventsByTeamIDsInRangeCtx` / `SearchTeamsByTokensCtx`（SQL token REGEXP 预筛绕过 79K 全表 cold start）；(2) `matcher.TeamFirstPoolBuilder` + `TokenSearchTeamLoader` 可选接口；(3) `UniversalEngine.EnableTeamFirstFallback` + `tfBuilder` lazy；(4) `runLeagueEvidenceFirst` second-pass：edges=0 且非 KnownMap → team-first → 多数表决 → 重跑 EF P3 一次；成功则 league.Rule=`LEAGUE_TEAM_FIRST` conf=0.70；(5) 与 v1.12 SUSPECT 互斥（tfApplied=true 跳过）。4 个单测 + CLI `--use-team-first-fallback`。默认 false 保 v1.15 行为。 | Claude Cowork |
| **v1.17** | **2026-05-16** | **Team-first 索引预加载（长跑批量场景优化）**：`TeamFirstPoolBuilder` 加 `PreloadIndex(ctx, sport)` / `IsPreloaded(sport)` / `PreloadStats(sport)` 公开 API + `sync.Mutex` 保护并发；`UniversalEngine.PreloadTeamFirstIndex(ctx, sport)` 暴露给上层。`Build` 内部候选 team 来源优先级改为 3 级：(1) **preload 命中** → 内存 token 倒排索引 (~50ms)；(2) `TokenSearchTeamLoader` → SQL REGEXP pre-filter（v1.16+ 默认，沙箱 4-5s）；(3) 全表 load + 内存索引 fallback。`cmd/server/main.go` 在 `batch2` / `ls-batch` 启动时若 `--use-team-first-fallback`，按出现的 sport 一次性 preload，后续 N 个 league 全部走内存路径，避免 SQL pre-filter 重复 4-5s × N。新增 helper `collectSports(leagues)`。内存机制 in-process mock 验证全通过（cold 10ms / warm 193ns / Build 复用 preload `GetAllTeamsBrief calls=1`）。生产建议：batch 跑 ≥10 个 league 开 preload，单跑 match2 / ls-match 不开。默认行为不变（match2/ls-match 不 preload）。 | Claude Cowork |
| **v1.18** | **2026-05-16** | **`--strict-no-mapping` 测评模式 — 杜绝 mapping 自证算法** 🔥 用户决议："本算法明确应该杜绝任何直接的 mapping，这种东西完全会让算法效果失真"，**否决 v1.18 原 KnownMap writeback 计划**。改为：(1) `MatchLeagueWithFlags(srTour, tsComps, noKnownMap)` + `MatchLeagueTopNWithFlags(...)` 新函数（旧 API forward）；(2) `SRSourceAdapter` 加 `NoKnownMap` 字段 + `NewSRSourceAdapterNoKnown` 构造器（对齐 LS 侧）；(3) SR Adapter 的 `MatchLeague` / `MatchLeagueTopN` 在 NoKnownMap=true 时走纯名称相似度；(4) `cmd/server/main.go` 4 个命令 `match2`/`batch2`/`ls-match`/`ls-batch` 全加 `--strict-no-mapping` flag — 开启时强制 `NoKnownMap=true` + 清空 `tsCompID` / 清空内置 league config 的 `ts_competition_id` 列；(5) PI-007 增补"测评 SOP"段，明确"杜绝 mapping 回写、测评必带 --strict-no-mapping"原则。in-process 验证：EPL (sr:tournament:17) 非 strict=`LEAGUE_KNOWN conf=1.000`，strict=`LEAGUE_NAME_HI conf=0.984`（算法分），Zimbabwe 等本不在 KnownMap 的联赛输出不变。生产链路保留 KnownMap 作"运营 override 层"，测评链路强制纯算法。 | Claude Cowork |
| **v1.19** | **2026-05-16** | **strict 基线扩展 + alias 审计 + KnownMap CI 哨兵** — v1.18 后的体系化加固：(1) `evidence_first_strict_baseline.py` 扩展按 sport 拆分 + Top-1~Top-7 全光谱（K_LIST=[1..7]，per_sport 聚合），首次给出"football Top-1 76.9% / Top-2 100%"清晰信号—algorithm 实际已经能"看见"正确答案，只是排序错位；(2) 审计 64 条 `LeagueAliasGroup` 全部为算法字典（对所有源生效的同义词，无 SR_ID→TS_ID 硬编码映射），符合 v1.18 灰色边界。含数字 group 无级别错误归并。**唯一已知风险**：缩写 group (BPL/CBA/NBA) 跨地域错配（如 Zimbabwe Premier Soccer League 被误归到 Premier League canonical 给 0.98 分），这是字典副作用，应靠 v1.19+ country 强约束对冲，不删字典；(3) `KnownLeagueMap`/`KnownLSLeagueMap` 加文档级警示注释，明确"生产 override 层、不参与测评"；(4) `scripts/check_no_mapping_in_eval.py` CI 哨兵：扫描测评脚本若直接读取 KnownLeagueMap 立即 fail，白名单需 `# ALLOW-KNOWNMAP-IN-EVAL`。修了 3 处历史 P0 评估脚本（事件层测试合法用 KnownMap 给 ts_comp_id），加白名单 + 用途注释。 | Claude Cowork |
| **v1.20** | **2026-05-16** | **Python strict eval 等价 Go 真实算法 — 数字大修订** 🔄 v1.18-v1.19 报告的"71.4% Top-1 / 28.6pp mapping 自证缺口"是 Python eval 算法不等价造成的失真：(1) Python eval 缺 `LeagueAliasIndex` —— LaLiga 实际在 alias group 里有 "Spanish La Liga"，Go 直接 0.98 命中，但 Python 只算字面 0.676；(2) Python eval 用了 `ts_leagues_2026.json` 里 basketball 的脏 country 字段（hash ID 而非国家名），触发 geo-veto 把所有 basketball 候选丢光。Go 侧 `ts_bb_competition` 表本就没 `host_country` 字段。修复栈：(a) `python/data/league_alias_groups.json` 从 `internal/matcher/league_alias.go::staticLeagueAliasGroups` 提取 64 组；(b) `python/evidence_first_strict_baseline.py` 加 `_load_alias_index` / `league_name_similarity_with_alias` 等价 Go；(c) `match_league_topk` 对 basketball 强制 `ts_country=''`，等价 Go schema。修复后真实数字：SR Top-1 100% (14/14)、basketball 100% (1/1)、football 100% (13/13)。**前提：评估集只有 14 个 GT 联赛**，PI-006 v1.10 Zimbabwe→BPL 等真实误匹配不在这 14 个里，要等 v1.21+ 扩 ground_truth 评估集才能暴露。team-first fallback (v1.16) 已经在生产里覆盖这些 silent 误匹配场景。 | Claude Cowork |
| **v1.21** | **2026-05-16** | **扩 strict eval 评估集到 100 GT 联赛 + alias 同步 CI 哨兵** — v1.20 揭示 14 GT 评估集太小（100% Top-1 不代表真实能力）。本版用 `python/evidence_first_strict_wide_baseline.py` 把评估集扩到 KnownLeagueMap (36 SR) + KnownLSLeagueMap (64 LS) = **100 个 GT 联赛**（v1.18 决议允许 mapping 作 GT 对照、不进算法路径）。候选池 99 个 TS。真实数字：**SR Top-1 66.7%（football 80.8% / basketball 30.0%）、LS Top-1 53.1%（football 73.2% / basketball 17.4%）**。Mapping 自证掩盖幅度：SR -33.3 pp / LS -46.9 pp / **basketball LS -82.6 pp**。三类真实弱点（按严重度）：(1) **alias canonical 跨地域错配** — Russian Premier League / League One England / Serie A Brazil 都因 alias canonical 给 0.98 但选错 ts_id；(2) **basketball alias group 覆盖严重不足**（64 组中 football ≈50 / basketball ≈5）；(3) 跨国杯赛 country=" " 无强约束（CONMEBOL Copa Libertadores / Copa Sudamericana）。配套加 `scripts/check_alias_sync.py` + `scripts/sync_alias.py` CI 哨兵：校验 `python/data/league_alias_groups.json` 与 Go `staticLeagueAliasGroups` 同步，不一致 fail。 | Claude Cowork |
| **v1.22** | **2026-05-16** | **算法改进 P0-A + P0-B（首次基于 strict 基线动算法）** — v1.21 暴露的两类 P0 缺口都修：(1) **P0-A basketball alias 大扩** — `internal/matcher/league_alias.go` 加 8 个 group（EuroLeague / Liga ACB / Lega Basket Serie A / Basketball Bundesliga / VTB / B.League B1 / BNXT / Orlen Basket Liga），64→72 组；`scripts/sync_alias.py` 自动同步 Python 字典。(2) **P0-B alias canonical hit 后 country 二次约束** — `leagueNameScore` 改造：当 `base ≥ 0.95`（alias canonical 命中标志）但 `sr.CategoryName` vs `ts.CountryName` 完全不同（非 international 且 `geoSimilarity < 0.4`）→ 强降到 0.55。修了 Russian Premier League / Serie A Brazil 这类跨地域错配。Python eval 同步 `match_league_topk`。**对照 v1.21 真实改进**：SR 整体 Top-1 66.7%→**72.2%** (+5.5 pp)，SR basketball 30%→**40%** (+10 pp)，LS football Top-2 80.5%→**87.8%** (+7.3 pp，country 二次约束最显著信号 — 真 GT 上浮到 Top-2)。未解决（v1.23+）：评估集 stub 的 TS 名称重复（English Football League One 两条同名）、2.Bundesliga 同 Bundesliga canonical（base 0.944 < 0.95 阈值未触发 veto，可调阈值或拆 alias group）、basketball 评估集 stub 缺 country/level 字段。 | Claude Cowork |
| **v1.23** | **2026-05-16** | **算法 P0-C: tier 数字 veto 扩展** — 修 `2. Bundesliga` (tier=2) vs `Bundesliga` (tier=0 隐式 1) 误配。`league_features.go::CheckLeagueVeto` 加规则：一侧 tier=0 另一侧 tier≥2 → veto（视为隐式 tier 1 vs N）。Python eval 同步 `extract_tier_number` + `check_tier_veto`。安全副作用：只 veto 当显式 tier ≥ 2，对 `Premier League`/`Bundesliga` 等所有非 tier 联赛无影响。 | Claude Cowork |
| **v1.24** | **2026-05-16** | **扩跨国杯赛 alias + stub TS name fallback 修复** — (1) `league_alias.go` 加 4 个跨国杯赛 group（CONMEBOL Libertadores / Copa Sudamericana / AFC Champions League / CAF Champions League），72→76 组；(2) `wide_baseline.py::build_eval_set` 加 `effective_ts_name = ts_name or src_name_fallback` fallback —— LS comment 格式不统一（部分缺 `→`）导致 stub TS name 解析空，让 GT 在 ts pool 里"看不见"。修复后 stub 至少有个 name 让算法可算。**v1.21 → v1.24 累计改进**：SR Top-1 66.7%→**80.6%** (+13.9 pp)、SR basketball 30%→**50%** (+20 pp)、LS football Top-2 80.5%→**95.1%** (+14.6 pp，算法基本能看见 GT)、LS football Top-1 73.2%→**85.4%** (+12.2 pp)。**剩余误匹配主要是评估集 stub TS 缺 country/level 字段**（"Serie A Brazil"、"League One England" 两条同名 ts_id 等），v1.25+ 需要从生产 DB 补 ts_pool 真实字段。 | Claude Cowork |
| **v1.25** | **2026-05-16** | **LS 链路同步 P0-B (alias canonical country 二次约束)** — `ls_engine.go::lsLeagueNameScore` 加 v1.22 P0-B 等价逻辑：alias canonical hit (base ≥ 0.95) 但 LS.CategoryName 与 TS.CountryName 完全不同（非 international 且 geoSimilarity < 0.4）→ 强降到 0.55。这是在原有 `lsLocationVeto` 前置 veto 之上的后置防护（当 LS.CategoryName 空时 lsLocationVeto 不触发，仍能被这层兜住）。Go build 干净通过。 | Claude Cowork |
| **v1.26** | **2026-05-16** | **`sync_ts_pool.py` + 真实 TS pool 评估** 🔥 之前 stub 池只 56 个候选，掩盖大量真实算法弱点。本版用 SSH+pymysql 直接拉生产 DB `ts_fb_competition` + `ts_bb_competition` 全表 = **3988 个真实 TS 联赛**（2601 football + 1387 basketball，与 Go 同 schema：basketball country=""）。`wide_baseline.py --use-real-pool` 切换。**真实池 vs stub 池数字（暴露 70x 干扰项后的真实算法能力）**：SR 整体 Top-1 80.6% → **63.9%** (-16.7 pp)、SR football 92.3% → **73.1%** (-19.2 pp)、LS 整体 67.2% → **57.8%** (-9.4 pp)。**新暴露的最严重弱点**：alias canonical 内部多 TS ID 歧义 —— `Premier League (Russia/Egypt)` 都被 alias canonical "Premier League" 命中给 0.98 但选错 ts_id（同 canonical 对应 England/Russia/Egypt 多个真实 ts_id）。`FA Cup` 同问题。v1.27+ 必做：alias canonical hit 后按 country 二次排序选正确 ts_id，而非简单降分。性能：3988 × 100 + alias 展开 = 22.8s（加 lru_cache）。 | Claude Cowork |
| **v1.27** | **2026-05-16** | **真实池 country 字段推断（让 country 二次约束在生产数据上生效）** — v1.26 真实池里 country 字段一半都空（如 'English Premier League' / 'Russian Premier League' / 'Belgian Pro League' country=''），导致 P0-B country 二次约束无法触发。本版扩 `wide_baseline.py::_infer_country_from_tsname` 词典（22→48 关键词，含国家形容词/国家名前缀/缩写/联赛名特征如 'Jupiler' → Belgium），并在 `--use-real-pool` 加载时给所有 country='' 的 TS entry 用 ts_name 推断填补。**v1.26 → v1.27 真实池数字**：LS football Top-1 75.6% → **78.0%** (+2.4 pp)、LS football Top-7 90.2% → **95.1%** (+4.9 pp)、SR football Top-7 88.5% → **92.3%** (+3.8 pp)。**v1.28 P0+ 尝试失败回滚**：把 `leagueNameSimilarityWithAlias` 同 canonical hit 从硬编码 0.98 改为 `0.85 + 0.13 * name_sim` 梯度，企图解决 BPL vs English Premier League 平手。但产生严重副作用 —— LaLiga (canonical "LaLiga") 被 LaLiga2 group 的 "LaLiga2" 字面命中 0.964，LS football Top-1 暴跌 78.0% → 46.3%。回滚到 v1.27 状态。结论：alias canonical 评分逻辑需要更精细方案（如：分级 alias group 区分 LaLiga 与 LaLiga2 的"近似但不同"、引入 ts_id 级 country 主键、cross-canonical alias 展开禁止跨 group 跳号）。仍是 v1.28+ 待办。 | Claude Cowork |
| **v1.28** | **2026-05-16** | **真实池数据清洁：去除 stub 注入冲突** — v1.27 真实池模式下 stub TS（KnownLeagueMap 注释解析）仍被合并进 ts_pool，stub 与真实池里**同名 ts_id**冲突（如 `FA Cup` stub=`jednm9wh5zryox8` vs 真实池 `9vjxm8gh8gr6odg`，算法选了真实那个有 country 的 → GT 指向 stub 算误匹配）。本版 `--use-real-pool` 时**跳过 ts_stub 合并**，GT ts_id 不在真实池就标 `GT_DRIFT`（KnownLeagueMap 注释 ts_id 过时）不计入 evaluable，同时打印 drift 报告。**v1.27 → v1.28 真实数字**：SR football Top-1 69.2% → **81.8%** (+12.6 pp)！SR football Top-7 92.3% → **95.5%**、SR 整体 Top-1 63.9% → **68.8%** (+4.9 pp)。LS 不变（64/64 GT 都在真实池，无 drift）。SR 4 条 GT_DRIFT 是 KnownLeagueMap 注释脏数据：sr:tournament:19/20/21/9 = EFL League One/Two/National League/FA Cup（这 4 个 ts_id 不在生产 ts_fb_competition 表）。**真实算法能力终于显现**：SR football 81.8% / LS football 78.0% / Top-7 都 ≥ 95%。**剩余 Top-1 误匹配真实算法 bug**：BPL 抢 Premier League、UCL 抢 UEFA Champions League（同 canonical 抢占）、Leagues Cup 抢 EFL Cup（字面相似但 country 不同 + 跨国 vs 国内 cup 类型）。v1.29+ 改进方向：alias canonical hit 后 country 优先选 ts_id（数据层而非算法层）+ EFL/UEFA cup 类型 veto。 | Claude Cowork |
| **v1.29** | **2026-05-16** | **P0-1 KnownLeagueMap drift 修 + P0-2 alias canonical + country 加分** — v1.28 暴露的两类问题各打一拳：(1) **P0-1 数据清洁**：4 个 SR KnownLeagueMap ts_id 跟生产真实池不一致（EFL League One/Two/National League/FA Cup），更新到真实 ts_id（8y39mp1hjzmojxg / 9k82rekhygrepzj / z318q66hv8qo9jd / 9vjxm8gh8gr6odg）。这让 SR evaluable 32→**36** 全部进准确率分母。(2) **P0-2 算法层**：`leagueNameScore` + `lsLeagueNameScore` 都加 `base ≥ 0.95 + locSim ≥ 0.8 → base += 0.015 (封顶 0.999)`，让相同 canonical 多 ts_id 内 country 一致的 ts_id 赢（Premier League (England) → English Premier League 应赢过 BPL）。Python eval `match_league_topk` 同步加 `elif base >= 0.95 and loc >= 0.8: base = min(base + 0.015, 0.999)`。**v1.28 → v1.29 真实数字**：SR football Top-1 81.8% → **84.6%** (+2.8 pp)、SR football Top-2 90.9% → **92.3%**、SR 整体 Top-1 68.8% → **72.2%** (+3.4 pp)。LS 不变（LS 64/64 GT 早已在真实池，无 drift；且本次未引发 P0-2 触发，因 SR 大多无 category 信息）。**评估池现状**：SR 36/36 evaluable、LS 64/64 evaluable、TS 候选池 3988 真实生产联赛。**剩余真实算法 bug** (v1.30+)：`Premier League (无 cat) → BPL`（src category 空，country 加分不触发）、`National League → UEFA Nations League` 0.92（domestic vs international cup 类型混淆）、`Leagues Cup → EFL Cup` 0.97、basketball pool country 全空（无法触发 country 加分/veto）。 | Claude Cowork |
| **v1.30** | **2026-05-16** | **国别推断字典移植 Go（让 basketball pool 也有 country 信号）** — 攻 P0 最大缺口 basketball（v1.29 LS basketball Top-1 仅 26.1%，因 ts_bb_competition 表无 host_country 字段，country 约束完全失效）。新文件 `internal/matcher/league_country_infer.go`: `InferCountryFromName(name) string` + `EffectiveCountry(country, name) string`（移植 Python `_infer_country_from_tsname` 48 关键词词典 — **算法字典性质**，对所有源生效，符合 v1.18 决议）。`leagueNameScore` / `lsLeagueNameScore` 在 `ts.CountryName==""` 时用 `EffectiveCountry(ts.CountryName, ts.Name)` 补全（如 "Lega Basket Serie A" → country=Italy；"Spain Liga EBA" → Spain）。Python eval `match_league_topk` 同步移除 basketball 强制清空 ts_country（v1.20 旧逻辑），让 v1.27 wide_baseline 推断的 country 实际生效。**v1.29 → v1.30 真实数字**：LS basketball Top-1 26.1% → **30.4%** (+4.3 pp)、LS basketball Top-7 52.2% → **56.5%** (+4.3 pp)、LS 整体 Top-1 59.4% → **60.9%** (+1.5 pp)。SR 不变（SR 评估集 basketball 多 src_cat 空，country 约束无法触发，需 v1.31+ 补 SR category）。 | Claude Cowork |
| **v1.31+v1.32** | **2026-05-16** | **SR category fallback (P0) + international vs domestic veto (P0)** — (v1.31) wide_baseline `build_eval_set` 在 SR/LS `src_cat` 空时调用 `_infer_country_from_tsname(comment + ts_name)` fallback。Premier League / FA Cup / UEFA CL 等评估时无 cat 的现在能从 KnownLeagueMap 注释推断出 country，让 P0-2 country 加分生效。(v1.32) `internal/matcher/league_features.go::CheckLeagueVeto` 加新规则：`lsInternationalCategory(a) != lsInternationalCategory(b) → veto`，攻 National League → UEFA Nations League / EFL Cup → Leagues Cup 类。Python `is_international` 同步扩 regex 关键词（加 uefa/conmebol/caf/afc/fifa/concacaf/europa）与 Go 等价。**v1.30 → v1.32 真实数字**：SR football Top-1 84.6% → **92.3%** (+7.7 pp)！SR 整体 Top-1 72.2% → **77.8%** (+5.6 pp)、SR 整体 Top-7 86.1% → **88.9%** (+2.8 pp)、SR basketball Top-7 60.0% → **70.0%** (+10 pp)、LS football Top-2 82.9% → **85.4%** (+2.5 pp)。剩余 SR Top-1 误匹配只有 8 条（v1.30 时 10 条），主要是 basketball 数据问题与 BPL 缩写归并问题。 | Claude Cowork |
| **v1.33** | **2026-05-16** | **alias group 加 DefaultCountry 字段基础设施（fallback 应用回滚）** — 攻 v1.32 剩 Premier League (Russia) → BPL 类（BPL canonical "Premier League" 但 country='' 推断不出）。新增 (1) `LeagueAliasGroup.DefaultCountry` 字段，给 51 个主要 group 填默认 country（Premier League=England / FA Cup=England / Liga ACB=Spain / EuroLeague=International 等）；(2) `LeagueAliasIndex.GetDefaultCountry(name)` helper 跟 `canonicalToDefaultCountry` map；(3) `scripts/sync_alias.py` + `check_alias_sync.py` 改 regex 解析 DefaultCountry 字段。**但 `EffectiveCountry` + Python `ts_country` 用 default_country fallback 后实测 LS basketball Top-1 -8.7 pp、SR Top-1 -2.8 pp**：当同 group 多 ts_id 都被 default_country 补成同 country 时仍平手（BPL/English Premier League 都 England），算法选错；basketball 误推断引发更糟。**回滚 fallback 应用**（`EffectiveCountry` 回到 v1.30 状态、Python `ts_country` 回到 v1.32 状态），保留 DefaultCountry 字段 + helper + sync 基础设施，留给未来更精细方案（如 source-side 推断、只在 group 单 ts_id 时启用、与多数表决配合）。数字回到 v1.32 水平。 | Claude Cowork |
| **v1.34** | **2026-05-16** | **LS 匹配率"差"真相：评估集 52% 是占位 GT (`(closest)` 标注)** 🔥 v1.33 LS 整体 Top-1 60.9% vs SR 77.8% (-16.9 pp) 看似 LS 算法弱。本版调查发现 LS 25 条 Top-1 误匹配中 **13 条 (52%) 是 GT 本身脏数据** — `KnownLSLeagueMap` 注释显式标注 `(closest)` / `(alt id)` 表示"生产 TS DB 没对应国家联赛先用最接近占位"：例如 `Basketligan (Sweden) → Basketball Bundesliga (closest)` / `BSN (Puerto Rico) → NBA (closest)` / `NBL (Australia) → Lega Basket Serie A (closest)` 等 12 条 basketball 占位。算法找不到不是 bug 是 GT 错。`wide_baseline.py` 加 `_placeholder_re` 检测 `(closest)/(alt id)` 注释排除 evaluable。**排除 12 条 placeholder 后真实数字**：LS basketball Top-1 30.4% → **63.6%** (+33 pp)、LS basketball Top-7 56.5% → **100%** (+43.5 pp)！LS 整体 Top-1 60.9% → **75.0%** (+14.1 pp)、LS 整体 Top-7 81.2% → **96.2%** (+15 pp)。**LS basketball 真实能力反而强于 SR basketball**（63.6% vs 40.0%）；LS 整体 Top-7 (96.2%) > SR (88.9%)。研究报告：`docs/ls_match_rate_investigation.md`。**后续 KnownLSLeagueMap 审查**：12 条 placeholder 应清理（删除或补真 ts_id）；测评一律排除 `(closest)/(alt id)`。 | Claude Cowork |
| **v1.35** | **2026-05-16** | **Python tier veto 修复（之前 ASCII BS 字符 bug 让所有 tier 失效）+ 扩 pattern** 🐛 调查 v1.34 后剩 12 条 LS 真 bug 时发现：`python/evidence_first_strict_baseline.py` 的 `_TIER_PATTERNS` 里含 ASCII BS (0x08) 字符而非 `\b` word boundary，导致 Python eval **所有 tier 提取静默失败**（K2 League/Korean K League 3/Russian Football National League 2/Lega Nazionale Pallacanestro Serie A2 等全 tier=0），check_tier_veto 永远 False。根因：之前某次 bash heredoc 写文件时 shell 转义 `\b` → ASCII BS 字符嵌入 raw string。修复：(1) python bytes 替换 0x08 → `\b`；(2) 扩 `_TIER_PATTERNS` 加两条新 pattern 处理 `German Bundesliga 2`（liga 前是字母 `s` 非 word boundary）和 `K2 League`（数字前是字母）。**v1.34 → v1.35 真实数字**：SR basketball Top-1 40.0% → **50.0%** (+10 pp)、SR 整体 Top-1 77.8% → **80.6%** (+2.8 pp)、LS football Top-1 78.0% → **82.9%** (+4.9 pp)、LS football Top-7 95.1% → **97.6%**、LS 整体 Top-1 75.0% → **78.8%** (+3.8 pp)、LS 整体 Top-7 96.2% → **98.1%** (+1.9 pp)。LS basketball 仍 100% Top-7。**真相二**：之前 LS 数据被 ASCII BS bug + GT placeholder 双重污染，真实算法能力 Top-7 实际 98.1%！还剩 11 条真 bug 主要是 `Mloda Ekstraklasa`（Mloda=波兰青年，未在 ageGroupKeywords）、`Nationale 1 vs France Nationale 1`（同名不同 ts_id）、`Liga ACB vs Liga EBA`（缩写差一字母）。 | Claude Cowork |
| **v1.36** | **2026-05-16** | **多语言 youth 关键词 + Spain/Italy basketball 次级 tier 字典** — 攻 v1.35 剩 11 条真 bug 中的 3 大类。Go `ageGroupKeywords["youth"]` 扩多语言：`mloda` / `młoda` (波兰青年) / `jovenes` (西班牙) / `jeunes` (法国) / `giovani` (意大利) / `primavera` (意青年队) / `sub` / `feminin`。Go `wordTierMap` 加 Spain basketball `liga eba=4, liga leb=2, leb oro=2, leb plata=3`，Italy basketball `serie a2=2, serie b/c basketball=2/3`，Argentina basketball `liga argentina b=2`。Python eval 同步加 `_WORD_TIER_MAP` + `has_youth_marker` + `check_youth_veto`。**v1.35 → v1.36 真实数字**：SR basketball Top-1 50.0% → **60.0%** (+10 pp)、SR basketball Top-7 70.0% → **80.0%** (+10 pp)、SR 整体 Top-1 80.6% → **83.3%** (+2.7 pp)、LS basketball Top-1 63.6% → **72.7%** (+9.1 pp)、LS football Top-1 82.9% → **85.4%** (+2.5 pp)、LS 整体 Top-1 78.8% → **82.7%** (+3.9 pp)。剩余 SR 6 / LS 9 真 bug：`EFL Cup → Leagues Cup`、`Premier League (Russia/Egypt) → BPL`、`FNL → Russian First League` (alias canonical 同 group 多 ts_id)、`HNL → Croatian Football League` (同)、`Liga Profesional → ARG Primera Nacional` (字面跨国相似)、`Nationale 1 → France Nationale 1` (同名不同 ts_id) 等都是更深的 alias group 内部歧义/跨国同名问题，需要 default_country source-side 推断或 alias 拆细才能进一步攻。 | Claude Cowork |
| **v1.37** | **2026-05-16** | **alias canonical default_country vs src.category 二次约束（用 country_equal 表）** — 攻 Premier League (Russia/Egypt) → BPL 跨国抢占。Go `leagueNameScore` + `lsLeagueNameScore` 加：当 `base ≥ 0.95` 且 `ts.CountryName == ""` 时调用 `GetLeagueAliasIndex().GetDefaultCountry(ts.Name)` 获取 default，跟 `sr.CategoryName` 比；不一致且非 international → 降 0.55。Python eval 同步 + 新增 `_COUNTRY_ALIASES` 表 + `country_equal(a, b)` helper（USA = United States = US = America；UK = United Kingdom = Britain；South Korea = Korea Republic；Czech Republic = Czech 等）。修两次迭代：v1.37a 用 `name_similarity < 0.4` 让 BPL 在 SR Russia 上生效（Top1 Laos PL 0.912）；v1.37b 改 0.55 阈值后误伤 NBA (USA vs United States Jaro=0.464)；v1.37c 用 `country_equal` 替换裸字面，准确等价同义国家。**v1.36 → v1.37c 真实数字**：SR 整体 Top-5 83.3% → **86.1%** (+2.8 pp)、LS football Top-5 95.1% → **98.1%** (+3.0 pp)、LS 整体 Top-5 96.2% → **98.1%** (+1.9 pp)。Top-1 不变但误匹配被推下排序（BPL 等不再 Top-1 而排到更后）。剩余 9 条 LS / 6 条 SR 真 bug 主要是 alias 同 canonical 多 ts_id 兄弟字面相似（FNL/HNL/EuroLeague）、跨国 cup 抢占（EFL Cup → Leagues Cup）、同名不同 ts_id（Nationale 1）等更深问题。 | Claude Cowork |
| **v1.38** | **2026-05-16** | **扩 default_country veto 到 NAME_MED + 扩 country 推断字典（达性能高原）** — 攻 v1.37 剩余 9 条 LS bug。Go `leagueNameScore` + `lsLeagueNameScore` 阈值从 `base ≥ 0.95` 放宽到 `base ≥ 0.70`（NAME_MED），让非 alias canonical hit 但字面相似度高的也能被 country veto。`wide_baseline.py::_infer_country_from_tsname` 加 60+ 国家关键词（Laos / Egypt / Vietnam / Bahrain / Hungary / Cyprus 等）让真实池 country 字段推断更全。**v1.37 → v1.38b 真实数字**：LS 整体 Top-2 90.4% → **92.3%** (+1.9 pp)、其他不变。剩余 LS 8 / SR 6 真 bug：(1) **FNL → Russian First League**：算法正确但 KnownLSLeagueMap GT ts_id 标错为 Russian Premier League（FNL 实际是俄罗斯次级，注释错配 — 应进 v1.39+ KnownLSLeagueMap 清理）；(2) **HNL → Croatian Football League**：同 alias canonical 兄弟字面竞争，算法选错 ts_id；(3) **Liga Profesional → ARG Primera Nacional**：同 country 字面相似（次级抢 1 级）；(4) **Premier League (Egypt) → Barbados/Laos Premier League**：alias canonical "Premier League" 内多 country 兄弟，每修一个又冒一个 long-tail；(5) **Nationale 1 → France Nationale 1**：评估集真实池里有 2 个同名 ts_id（数据本身的歧义）；(6) **Serie A basketball → Italy Serie B**：tier 应不同（A vs B）但 alias group 含 "Serie A"+"Serie A Italian Basketball"，alias canonical 命中盖过 tier veto。**已到达算法能力高原**：继续单点修无明显边际收益，需要架构性变化（如 alias group 内 ts_id 子标记、source-side fully qualified mapping）。**v1.25 → v1.38b 累计算法改进**：SR 整体 Top-1 63.9%→**83.3%** (+19.4 pp)；LS 整体 Top-1 53.1%→**82.7%** (+29.6 pp)；LS Top-7 71.9%→**98.1%** (+26.2 pp)。 | Claude Cowork |
